PORT=4000
FRONTEND_URL=http://localhost:5173
DATABASE_URL="file:./dev.db"
JWT_SECRET="replace-this-with-a-long-random-secret"
COOKIE_SECURE=false
CONTACT_EMAIL=jhasachin0661@gmail.com

# Admin login used by the seed script. If ADMIN_PASSWORD is left blank, a
# random one-time password is generated and printed once when you run
# `npm run db:seed` — it is never committed or hardcoded.
ADMIN_EMAIL=admin@portfolio.local
ADMIN_PASSWORD=
# Gmail: use an App Password, never your normal account password.
SMTP_SERVICE=gmail
SMTP_USER=jhasachin0661@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=jhasachin0661@gmail.com
